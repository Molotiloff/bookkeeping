/**
 * Placeholder for generated FastAPI OpenAPI types.
 *
 * When backend exposes /openapi.json, this file should be replaced by
 * openapi-typescript output and imported by DTO adapters instead of hand-written
 * page contracts.
 */
export type OpenApiPaths = {
  readonly __placeholder?: never;
};

export type OpenApiPath = keyof OpenApiPaths;
